<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant-rater</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<header>
    <h1 class="title is-1">Restaurant Rater</h1>
</header>
<br>


{{!-- Use media object to style with bulma --}}
<body>
    <div class="wrapper">
        <header>
            <div>
            <nav class=" columns nav">
                <a class="box column link" href="/">Home</a>
                <a class=" box column link" href="/dashboard">Dashboard</a>
                {{#if loggedIn}}
                <a class=" box column link" href="/" id="logout">Logout</a>
                {{else}}
                <a class=" box column link" href="/login">Login</a>
                {{/if}}
            </nav>
            </div>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer class="class-footer">
            <p class="card-footer-item">Thanks for visiting!</p>
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>

