<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant-rater</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

{{!-- Use media object to style with bulma --}}
<body>
    <div class="wrapper">
        <header>
            <div>
            <nav class="nav">
                <a class="link" href="/">Home</a>
                <a class=" box link" href="/dashboard">Dashboard</a>
                {{#if loggedIn}}
                <a class=" box link" href="/" id="logout">Logout</a>
                {{else}}
                <a class=" box link" href="/login">Login</a>
                {{/if}}
            </nav>
            </div>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer>
            Thanks for visiting!
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>